# The per-commit userflow run: `mvn verify` at the pushed commit — which regenerates
# service/target/userstories/ through the five story classes — and the reports published as one
# docs bundle, site @userflows/qits-maintenance-platform-service, version = the bare commit sha, branch
# riding as artifacts metadata for discovery.
#
# NON-GATING BY DESIGN, which is why it is a separate file from ci-post-receive.yml: this run going
# red must not cost the branch its image while "quality gate before merging" is still a decision to
# be taken — when it is, these per-commit runs are what the gate reads. One run per push per
# branch, collapsed to the newest tip under burst. It selects every tracked branch, maintenance/*
# included: a bump branch is exactly a commit whose pins moved, and proving it is what this file is
# for. The release leg that only a maintenance/* branch gets stays in ci-post-receive.yml, where a
# push to one is already something this repository reacts to.
#
# THIS IS THE FIRST TIME THIS REPOSITORY'S SUITE RUNS IN CI AT ALL, and that is worth knowing before
# reading anything below. ci-post-receive.yml builds the image and nothing else: the Dockerfile's
# mvnw line carries `-DskipTests`, so until this file existed every test here had only ever run on a
# developer's machine. `verify` here is the WHOLE reactor — `maintenance` (the parsers, the pending
# rule, the store) and `service` (the API, the schedules, the packaged surface) — not the one IT.
#
# `user: build`, and it is zonky's reason. Both modules spawn a real PostgreSQL from a maven
# artifact — the clone-alone, docker-free rule is why the store is not a container — and initdb
# refuses to run as root and calls getpwuid, so the step needs a passwd-backed non-root user.
# maven-base carries one (adduser -D, uid 1001). The step containers are Alpine (musl), which is
# why BOTH poms that name zonky architectures carry embedded-postgres-binaries-linux-amd64-alpine as
# a fourth arch: a missing arch resolves green and then dies in the middle of a suite. A step
# container is started --cap-drop=ALL either way; /workspace is 1777 with a system-level
# safe.directory entry, so a non-root checkout is what the image was built to serve.
#
# NO DOCKER SOCKET IS WANTED. Nothing in this repository shells out to a daemon — this service
# decides what changes and a CI step applies it — and the store is a child process, not a container.
# `docker: true` would be a grant for nothing.
#
# NO NETWORK IS WANTED EITHER, and in the repository whose whole subject is other repositories that
# is worth spelling out. The story catalogue stands FIVE peers up itself: stories/support/StoryPeers
# is a com.sun HttpServer on a loopback port per service — qits-projects, qits-githost, qits-ci,
# qits-platform-artifacts, qits-platform-mirror — armed by StoryCatalog with a two-repository
# platform's manifests and metadata, and the launched fast-jar is handed their addresses as its
# eight shipped `qits.maintenance.*-url` keys. So a scan in this step reads a real socket with a real
# HttpClient and reaches nothing outside the container. That is what makes every peer hop an
# OBSERVED edge with the status that came back on it, including `-> dropped` for a git host the
# outage story turns off mid-story; the single edge that stays declared is the JDBC read of this
# service's own postgres, which no tap out here can stand in front of.
#
# Quinoa is off (`-Dquarkus.quinoa=false` — the extension's root toggle; `.enabled` is silently
# ignored and npm still runs): the client is ci-post-receive.yml's business, where the
# qits-maintenance-platform-frontend submodule is initialised and its lockfile origins are swapped
# against the platform's npm registry, and none of that ritual belongs here — the stories assert
# nothing about the SPA. It is also what keeps this step from needing the submodule at all: the
# daemon's clone does not recurse, so service/src/main/webui arrives EMPTY, and an empty directory
# is the one case Quinoa treats as a misconfiguration rather than as "no client"
# (application.properties, the service pom and docker/Dockerfile's `test -f` guard all say the same
# thing from their own side). The packaged fast-jar failsafe boots is otherwise exactly what ships.
#
# THE TWO IT FLAGS ARE THE OTHER HALF, and they are why this repository does NOT opt its service
# module back into ITs the way qits-githost does. `skipITs` is true in the root pom and stays true;
# this module has six integration tests and exactly one of them does not belong to this run:
#
#   TokenValidationBootstrapIT  the catalogue's first class — the packaged boot with the OIDC tenant
#                               ON, which is also the only place it is ever on
#   ScanCycleIT                 where the inventory comes from: every manifest at one commit, every
#                               registry asked by KIND, and a git host that goes dark mid-story
#   InventoryIT                 what an operator reads, and the five arrows that are not there
#   BumpIT                      the one thing this service makes happen anywhere else — a payload
#                               for somebody else's pipeline, and the two endings a green run has
#   MaintenanceRefusalIT        the doors: 401, 403, and the requests that start no work
#   PackagedSurfaceIT           NOT in the list: half about the CLIENT (the base href at the root,
#                               the deep links, the fallback that must not swallow /maintenance),
#                               which the quinoa line above deliberately does not build. A blanket
#                               `-DskipITs=false` would make this run red on a test that is right.
#
# THE LIST IS THE CATALOGUE, BY NAME, AND A NEW STORY CLASS JOINS IT IN THE SAME COMMIT — an
# unlisted class is silently never run and its diagram silently never published. Its ORDER here is
# not the run order: failsafe takes the classes as a set, and what orders them is
# UserflowClassOrderer, registered as junit's secondary orderer in the test application.properties
# and driven by each story's @UserflowRunsAfter. That ordering is load-bearing rather than tidy —
# every far side is a cumulative recording attributed by a cursor, so the startup JWKS fetch lands
# in whichever story drains FIRST, which the ordering makes TokenValidationBootstrapIT's first
# story; and the reading stories can only claim "no peer was asked" once the scan that DID ask has
# drained its own edges.
#
# Naming the classes also keeps the property's own meaning ("run everything") intact for the
# `native` profile in service/pom.xml that sets it.
#
# NO REPAIR WAS NEEDED to make the two new test-scope jars resolve. This repository already carries
# `.qits-maven-settings.xml` and a `qits-maven` <repositories> entry — qits-db-core, qits-auth-core
# and qits-arch-rules have always come from the platform registry — and docker/Dockerfile already
# builds with `--network host` and `-s .qits-maven-settings.xml`, so the image builds driven by
# ci-post-receive.yml and ci-event-release.yml resolve the new test classpath there the same way
# they already resolved those three. (`-DskipTests` skips test EXECUTION, not test COMPILATION, so
# the jars are fetched during an image build too; they are small, and qits-userflows' transitive
# Playwright is a jar, not a browser — nothing downloads Chromium unless a story asks for a Flow,
# and neither of these does.)
#
# Loop review: this pipeline PUTs a docs bundle and pushes no commit — no cycle, and in this
# repository that sentence is worth checking twice, because a maintenance BUMP is how commits get
# pushed elsewhere and one of the stories below drives exactly that. THE REAL qits-ci IS NEVER
# ASKED: `qits.maintenance.targets.ci-url` is handed to the launched process pointing at the
# StoryPeers stand-in on a loopback port, so what BumpIT triggers is a fixture that answers with a
# run id and nothing evaluates a pipeline anywhere. The same holds for the other four peers. The
# bundle goes to qits-artifacts and is read back by qits-docs, neither of which is this service.
#
# The CLOCK is the other thing to check, and it is not simply off here: a bump is closed by the
# sweep and by nothing else, so StoryProfile leaves quarkus.scheduler.enabled ON and removes every
# other timer at its own shipped key — both scan crons are `off` (the scheduler's own value for
# "do not register this trigger") and qits.maintenance.scan.enabled is false, so no clock ever
# starts a scan, and the sweep is a no-op whenever no bump is in flight. Two paths are therefore not
# covered by a story and keep their coverage in MaintenanceApiTest: a SCHEDULED scan asking for the
# bumps it found, and RestartRecovery resuming one across a restart.
#
# There is nothing else to darken: this repository has no event bus client and no opentelemetry
# extension on its classpath, and the one dial-out a boot with the gate on makes is the JWKS fetch,
# which the profile points at its own MockIdp. Publish-if-absent: events redeliver and docs versions
# are immutable, so 409 is success (the qits-spa-ui-components release file's discipline).
event: SCMPublishCommit
when:
  # repoName, not repoId: since the identity cutover the git host's own coordinate is an opaque
  # UUID, so a name match belongs on the name. The event echoes the public name the push was
  # addressed with; a rename updates this line in the same campaign.
  - repoName: { exact: qits-maintenance-platform-service }
    suppressCi: { exact: "false" }
checkout:
  branch: branch
  sha: sha
steps:
  - image: qits/build-images/maven-base:latest
    user: build
    timeout-seconds: 2400
    script: |
      set -eu
      # The platform registry, exactly as qits-eventstream's step derives it: the pom's default
      # names a host-published address unreachable from a step container, and the exact-id mirror
      # in .qits-maven-settings.xml is what passes Maven's HTTP blocker.
      export QITS_MAVEN_REPOSITORY_URL="$QITS_MAVEN_REGISTRY_URL"
      # Maven Central through the platform mirror's in-network alias — empty (an older qits-ci that
      # injects nothing, or a deployment that turned the mirror plane off) deactivates the settings
      # profile and Central is resolved directly.
      export QITS_MAVEN_CENTRAL_URL="${QITS_MAVEN_PROXY_URL:-}"
      # -Dquarkus.http.test-port=0 is AGENTS.md's gate command spelled out rather than assumed: both
      # modules' test application.properties already pin it and failsafe passes it to the launched
      # artifact, so this repeats what is configured rather than adding to it — and it is the one
      # flag whose absence turns "8081 is already bound" into something that reads like a code
      # failure.
      ./mvnw -B -ntp -s .qits-maven-settings.xml verify \
        -Dqits.maven.repository.url="$QITS_MAVEN_REPOSITORY_URL" \
        -Dquarkus.http.test-port=0 \
        -Dquarkus.quinoa=false \
        -DskipITs=false \
        "-Dit.test=TokenValidationBootstrapIT,ScanCycleIT,InventoryIT,BumpIT,MaintenanceRefusalIT"

      # `-C … .` so the story directories are the bundle's roots — nesting one directory deep would
      # put it in every served URL. The path is service/target/userstories because the stories live
      # in the one module that packages an artifact to boot; `maintenance/` is a library jar.
      tar -czf userstories.tgz -C service/target/userstories .

      # Branch and commit ride as metadata (the docs store's exact-match discovery keys); the
      # version is the sha itself, so a bundle URL never changes meaning.
      status=$(curl -sS -o /tmp/docs-publish.out -w '%{http_code}' \
        -X PUT --data-binary @userstories.tgz \
        -H "X-Artifacts-Meta-git.branch.name: $QITS_CI_BRANCH" \
        -H "X-Artifacts-Meta-git.commit.hash: $QITS_CI_SHA" \
        -H "X-Artifacts-Meta-git.repository.name: qits-maintenance-platform-service" \
        "$QITS_DOCS_URL/@userflows/qits-maintenance-platform-service/-/$QITS_CI_SHA")
      case "$status" in
        201) echo "published userflows for qits-maintenance-platform-service@$QITS_CI_SHA ($QITS_CI_BRANCH)" ;;
        409) echo "userflows for qits-maintenance-platform-service@$QITS_CI_SHA already published — skipping" ;;
        *)   echo "docs publish failed: HTTP $status"; cat /tmp/docs-publish.out; exit 1 ;;
      esac
